# ==============================
# Runtime Config (for API testing)
# ==============================
# 배포 완료 후 end-user가 APIM을 통해 모델을 호출할 때 필요한 설정
# (Settings needed for end-users to call models through APIM after deployment)
# 인프라 배포 설정은 infra/terraform.tfvars.example 참조
# (For infrastructure deployment settings, see infra/terraform.tfvars.example)

# APIM Gateway endpoint
APIM_ENDPOINT=https://<apim-name>.azure-api.net

# Personal Key (Developer Portal에서 구독하여 발급받은 키)
# (Personal Key obtained by subscribing on the Developer Portal)
# 또는 Service Key (CI/CD용 — terraform output -json apim_subscription_keys 로 확인)
# (Or Service Key for CI/CD — check with: terraform output -json apim_subscription_keys)
APIM_SUBSCRIPTION_KEY=<your-personal-key>

# 사용할 모델 배포 이름 (Model deployment name)
DEPLOYMENT_NAME=gpt-4o

# API 버전 (API version)
API_VERSION=2024-12-01-preview
